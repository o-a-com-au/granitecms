# Phase 3 acceptance checklist - admin application

Rules for using this file (same as Phase 1 and 2):

- An item is done only when its proving test exists, passes, and is named in the "Proof" column. "It works when I run it" is not proof.
- Work top to bottom within a group; groups are ordered by dependency.
- When all items in a group are proven, commit with message `phase3: <group> complete`.
- Do not edit the criteria to match the implementation. If a criterion is wrong, raise it with the lead first.
- Each group gets its own working session: research any new dependency/API empirically before designing, a plan-mode design review for anything with a real architectural decision, then implement in small commits.

This is a first-pass draft, written before any admin code exists. Unlike Phase 1/2's checklists (refined session by session against a codebase that already existed one phase earlier), several criteria here will firm up - or change - once their own group's design session actually researches the chosen tooling. That is expected, not a sign the draft was wrong; each group should feel free to revise its own not-yet-started rows before implementing them, same as any other group would flag a wrong criterion to the lead.

The admin lives in its own repository (`app-granite-cms-admin`, sibling to this one, empty at the time of writing), per the build plan's constraint that the admin and the site are always two separate codebases with two separate deploy cycles. This checklist stays here, alongside the build plan it's derived from, matching where Phase 1 and 2's checklists already live.

## Open questions carried in from scoping (resolve at the named session, not before)

1. ~~Admin persistence mechanism~~ **Resolved in Group A: a JSON-file-backed `Store<T>` interface** (`packages/server/src/store/` in the admin repo). Not modelled on this repo's own SQLite driver-interface precedent - that exists because a second real implementation (`better-sqlite3`) already exists and the data (the search index) is disposable/rebuildable; neither is true of the admin's registry/account data, which is small-volume and authoritative. The interface's real job is testability (an in-memory fake for later groups' route tests), with "could swap to SQLite later" as a true but secondary bonus - so no grep-enforced single-implementation test was added, unlike this repo's B7/G5 precedent. Atomic writes (temp file + rename) and a promise-chain write-queue mutex are built in from day one, since this data is authoritative, not derived.
2. ~~Admin login mechanism~~ **Resolved in Group B: username/password against the Store-backed users collection.** No OAuth/magic-link - matches the self-hosted/free-tier simplicity bias, and SSO is explicitly an Enterprise-tier feature for later (per the commercialisation strategy), not something to build speculatively into Phase 3. Hashed with `node:crypto`'s `scrypt` + `timingSafeEqual`, no new dependency.
3. ~~Session mechanism~~ **Resolved in Group B: real server-side sessions** (`@fastify/session` + `@fastify/cookie`, backed by the admin's own `Store<T>` via a small callback-adapter), not a stateless signed cookie - chosen for genuine server-side revocation on logout, confirmed directly with the user against the alternative's lower cost (one dependency instead of two, no server-side store).
4. ~~Test tooling for the admin repo~~ **Resolved in Group A: a split, not a preference.** `node --experimental-strip-types --test` (matching this repo exactly) for the admin's backend, but Vitest + React Testing Library + jsdom for frontend component tests - forced, not chosen: `--experimental-strip-types` only erases type annotations, it does not transform JSX, so a `.tsx` file cannot run under bare `node:test` at all. Real end-to-end browser tests (mirroring this repo's `page-reviewer` precedent) remain undecided and deferred - no group has needed one yet.
5. Diff rendering for the history view (Group H): a real diff needs a real diffing library (line-level, at minimum) - which one, and how much of a merge-request-style UI it justifies, is a Group H research question, not decided here.

## Group A: admin app bootstrap

| # | Criterion | Proof |
|---|---|---|
| A1 | The admin app builds and boots as a standalone Vite/React project, with its own `package.json`, entirely separate from the agent repo | `app-granite-cms-admin` repo (sibling, not this one): `npm run build && npm start` then a real `fetch`/`curl` against the compiled single-process boot - verified live (`GET /`, `GET /api/health`), not just unit-tested |
| A2 | `npm run typecheck`, `npm run lint`, and a test runner are configured and pass on an empty/skeleton app, mirroring the agent repo's definition-of-done gate | `app-granite-cms-admin`: `packages/server/test/server.test.ts :: A2: GET /api/health returns ok`; `packages/web/test/App.test.tsx` (Vitest); root `npm run typecheck`/`lint`/`test` fan out across both workspace packages and all pass |
| A3 | A chosen persistence mechanism (open question 1) stores and retrieves at least a trivial record round-trip, with a justification recorded for why it was chosen over the alternative | `app-granite-cms-admin`: `packages/server/test/store/json-file-store.test.ts :: A3: a trivial record round-trips through save, list, find, and delete`, plus atomic-write and concurrent-write-queue tests in the same file; justification recorded as a comment in `packages/server/src/store/store.ts` |
| A4 | The app has no compiled-in knowledge of any specific site: every site interaction goes through a base URL and token read from the app's own persisted configuration, never a hardcoded value | `app-granite-cms-admin`: `packages/server/test/static/static-analysis.test.ts :: A4: no hardcoded http(s) URL literal exists in packages/server/src outside a reasoned allowlist`, plus its own mechanism-check positive control (mirroring this repo's B4/H2 precedent) - passes vacuously today since Group C hasn't added site-calling code yet, by design |

Design notes:

- **The admin gets its own lightweight Node backend (Fastify), not a pure browser SPA** - a real architectural gap in the original build plan, caught before any code was written: it only sketched frontend folders, but a shared, multi-user site registry holding real secrets (per-site API tokens) and real login sessions can't safely be browser-only storage. The backend proxies every site call, so a registered site's raw API token never has to reach the browser at all - confirmed directly with the user rather than assumed, given a genuine alternative (a third-party auth/BaaS provider) existed and was rejected as introducing an external vendor dependency for a tool whose whole pitch is "own your stack."
- **Two npm workspace packages (`packages/server`, `packages/web`), one repo** - a real split, not premature structure: Node/NodeNext and browser/bundler-resolution-with-JSX are genuinely different compilation targets with different dependency sets.
- **A real CVE caught before it shipped, not by inspection**: the first pass pinned `@fastify/static` at `^8.1.0` (matching what was available when the plan was drafted); `npm audit` immediately flagged four high-severity path-traversal/route-guard-bypass advisories against 8.x/9.x. Re-pinned to `^10.1.2` (confirmed via `npm view` to have no peer-dependency conflict with Fastify 5) before writing any code against it - path/traversal safety is treated with the same seriousness here as the agent repo's own `sanitisePath`.
- **`@vitejs/plugin-react`'s Babel variant, not the SWC variant** - the SWC variant ships a native Rust binary, the same "native module pain" this project already steered around by choosing `node:sqlite` over `better-sqlite3`.
- **The built web assets' default location is resolved from the server module's own location (`import.meta.dirname`), not `process.cwd()`** - deliberately the opposite convention from the agent's own "never resolve relative to the package location" rule (constraint 2 there), because the two situations aren't the same: locating a *fixed sibling build artifact* (`packages/web/dist`, always two directories under `packages/` from either source or compiled `packages/server/{src,dist}`) is a structural fact independent of deployment, unlike the agent's site *content*, which is genuinely external, per-deployment configuration. The admin's own data directory (site registry, accounts) stays `process.cwd()`/`ADMIN_DATA_DIR`-relative, since that genuinely is operator configuration - the two defaults are deliberately resolved differently, for different reasons, not an inconsistency.
- **A real port collision caught by actually running it, not by inspection**: the default dev port (originally 4000) turned out to already be bound by an unrelated pre-existing local process on the development machine. Moved to 4278 after verifying the dev-mode Vite-proxy-to-Fastify wiring end to end with real requests.
- **Stop-gate hooks are per-project and were ported, not inherited** - `app-granite-cms-admin` has its own `.claude/settings.json`/`.claude/hooks/`, adapted from this repo's (the staleness check's `find` glob changed from a flat `src test` to `packages/*/src packages/*/test` for the two-package workspace layout). Its own `CLAUDE.md` is adapted, not copied: keeps TypeScript strict mode, Australian English/no em-dashes, small commits, and the three-part definition of done, but drops everything specific to the site agent itself (content-is-JSON-files, site-root-from-config, `node:sqlite` mandate, no-Liquid-tags rule) and references this repo's build plan/checklist by relative sibling path rather than duplicating them - confirmed directly with the user that the two repos stay fully separate (no parent monorepo), after weighing that option's cost (this session's working directory would stop being "the real one," and it becomes easier to accidentally import across the admin/agent boundary instead of going through the HTTP API) against its benefit (shared docs/`.claude` without cross-repo bookkeeping).

## Group B: admin authentication

| # | Criterion | Proof |
|---|---|---|
| B1 | An unauthenticated visitor is redirected to a login screen for every route except login itself | `app-granite-cms-admin`: `packages/web/test/App.test.tsx :: B1: an unauthenticated visitor at / is redirected to the login screen`, `:: B1: /login itself is reachable while unauthenticated`; `packages/web/test/auth/RequireAuth.test.tsx` (redirect/render/loading-state unit coverage) |
| B2 | A correct login succeeds and establishes a session (mechanism per open question 3); an incorrect login is rejected with a clear error, no information leak about which field was wrong | `packages/server/test/routes/auth.test.ts :: B2: a correct login succeeds and establishes a session`, `:: B2: an unknown username is rejected with 401 and a generic message`, `:: B2: a wrong password is rejected with 401 and the identical generic message`; `packages/web/test/pages/LoginPage.test.tsx` (client-side message indistinguishability) |
| B3 | A logged-in session persists across a page reload; logging out ends it, and a subsequent request in that browser is treated as unauthenticated | `packages/server/test/routes/auth.test.ts :: B3: a logged-in session is usable on a later request`, `:: B3: logging out ends the session - a subsequent request with the same cookie is unauthenticated`. Also verified live, not just unit-tested: a real `npm run build && npm start`, bootstrap credentials read from the real startup log, a full curl+cookie-jar login/me/logout/me cycle against a real process, and the same pre-restart session cookie confirmed still valid after killing and restarting the process (the exact scenario the persisted session secret exists to solve) |
| B4 | The authenticated user's identity is available to every later group that needs to pass it through as commit author (build plan: "the admin passes the authenticated editor's identity with each request") | `packages/server/test/routes/auth.test.ts :: B4: the authenticated identity is available to a downstream route via requireAuth` - a throwaway `GET /api/__whoami-gated` route registered directly in the test, mirroring this repo's own Phase 2 precedent for proving a mechanism no real downstream route needs yet |

Design notes:

- **A permanent empirical smoke test before any route code, not README-trust**: `@fastify/session`'s docs don't fully specify whether `session.regenerate()` exists on the installed version or whether a custom store is callback- or promise-based. A dedicated test registering both plugins on a real Fastify instance settled both (regenerate exists and issues a new session id; the store contract is Node-style `(id, session, callback)`) before `session-store-adapter.ts`/`require-auth.ts` were written against an assumption - mirrors this repo's own precedent of never trusting a new dependency's interop without running it for real first (`@fastify/rate-limit`, Group A's `@fastify/static`).
- **A real TypeScript finding, not just a test result**: `@fastify/session`'s `Session` type only carries its own internal cookie options by default - `request.session.set('userId', ...)` doesn't typecheck without declaring custom session data via module augmentation (`packages/server/src/auth/session-types.ts`), found from a real `TS2345` error while writing the smoke test.
- **Two username-enumeration channels closed, not one**: B2's literal wording only names a message leak, but an unguarded implementation (skip password verification entirely when the username doesn't exist) leaks the same information through response *timing* instead - `scrypt` is deliberately slow, so skipping it for an unknown user is measurably faster. Fixed with a fixed `DUMMY_HASH`/`DUMMY_SALT` pair so `verifyPassword` always runs, win or lose.
- **`POST /api/auth/logout` is deliberately exempt from the auth guard, not an oversight**: guarding it would 401 exactly the already-expired/invalid session state logout exists to clean up. `GET /api/auth/me`, conversely, is *not* an exemption - it IS B1's mechanism (the frontend calls it to decide whether to redirect), so a 401 there is the guard working correctly, not a gap.
- **The auth-guard static-analysis test refines this repo's own `H2` fixed-character-window precedent, not a straight copy**: `routes/auth.ts` genuinely mixes an exempt route (login, logout) with a guarded one (me) in the same file, which would make a file-level "does `requireAuth` appear anywhere" check (this repo's own `B4` shape) pass trivially without ever catching a route that forgot the guard. Each route's window is bounded by the *next route registration* in the same file (or EOF), not a fixed character count - immune to the false-negative risk a fixed window has if a neighbouring route's guard declaration happens to fall within it. Verified two ways before being trusted: a synthetic mechanism check reproducing the exact exempt-then-guarded adjacency `routes/auth.ts` has, and a real scratch violation temporarily added to `routes/health.ts`, confirmed to fail the test, then reverted.
- **CSRF posture reasoned through explicitly, not left implicit**: cookie-based sessions reopen CSRF, which the site agent's bearer-token auth never had to consider. Verified empirically (not assumed) that Fastify's own default body parser rejects a form-encoded or multipart POST to a JSON-only route with 415 - combined with `SameSite=Lax` (blocks cookie attachment on cross-site mutations) and no `@fastify/cors` registered (blocks a genuine cross-origin JSON fetch via a failed preflight), this closes the one CSRF shape neither of those alone stops, without a dedicated CSRF-token library. Flagged forward, not fixed now: the first route accepting `multipart/form-data` for a real reason (asset uploads) reopens this exact hole and needs its own mitigation then.
- **A real CVE found and fixed via the audit trail, not the advisory's severity label alone**: `npm audit` flagged `react-router-dom`'s `react-router` dependency (7.12.0-8.2.0) for a CSRF advisory. Read the actual advisory rather than reacting to the label - it's scoped to unstable RSC server-action code paths this app doesn't use (plain `BrowserRouter`/`Routes`, no data router, no server actions), so it wasn't really exploitable here. Upgraded anyway to `react-router` v8.3.0 directly (v7 merged `react-router-dom` into the main package; only the import source changes, no API changes for plain declarative routing) rather than carry a flagged version while writing a "this is fine, actually" justification - a clean, compatible upgrade path existed (`react`/`react-dom` 19.2.8 already satisfy v8's `>=19.2.7` peer requirement), matching the same instinct that drove Group A's `@fastify/static` re-pin.
- **A real test-isolation bug, found while writing these tests**: `@testing-library/react`'s automatic `afterEach` cleanup only self-registers when it detects a *global* `afterEach`, and this repo deliberately doesn't set Vitest's `globals: true` (imports stay explicit everywhere). DOM from one test was silently leaking into the next within the same file (multiple "Log in" headings, multiple "Username" fields found by RTL's queries) until a `test/setup.ts` calling `cleanup()` was wired in via `vite.config.ts`'s `test.setupFiles`.
- **First-admin bootstrap** (open question, resolved directly with the user): auto-generates a username/password on first boot if the users store is empty (or uses `ADMIN_BOOTSTRAP_USERNAME`/`PASSWORD` if set), logged to stdout exactly once - mirrors this repo's own `create-site` "a real starter auth token is generated... printed once, never written to disk" precedent. Deliberately not called from `buildServer` (which must stay side-effect-free for tests); only `index.ts`'s real boot sequence runs it, verified live against a real fresh boot, not just asserted.

## Group C: site registry

| # | Criterion | Proof |
|---|---|---|
| C1 | An operator can register a site by URL and API token; the registry stores it and lists it thereafter | `app-granite-cms-admin`: `packages/server/test/routes/sites.test.ts :: C1, C2: registering a site stores it, lists it, and shows real live status`; `packages/web/test/pages/HomePage.test.tsx :: C1: registering a site adds it to the list`. Verified live end to end: a real scaffolded agent site (`create-site`, packed via `npm pack`, installed from the tarball, booted for real on its own port) registered against a real running admin via curl - not a fixture or mock on either side |
| C2 | Registering a site calls `GET /v1/capabilities` and displays the agent package version, content schema version, and active SQLite driver against that entry | `packages/server/test/routes/sites.test.ts` (same test as C1 - the registration response itself carries the live status); `packages/web/test/pages/HomePage.test.tsx :: C2: the newly registered site shows its live status`. Verified live: the real scaffolded site's actual `{agentVersion, contentSchemaVersion, sqliteDriver}` appeared in the registration response |
| C3 | A token can be rotated for a registered site without losing the site's other registry data | `packages/server/test/routes/sites.test.ts :: C3: rotating a token updates only the token, proven live against the fake site, and keeps other fields`; `packages/web/test/pages/HomePage.test.tsx :: C3: rotating a token submits the new value and refreshes the row`. Verified live: rotating the real site's registry entry to a wrong token flipped its live status to `unauthorized`, then rotating back to the real token flipped it back to `ok` - proving the stored token is what is actually used on every check, not cached from registration |
| C4 | A site can be removed from the registry; removal never touches the site itself (the registry is metadata only, holds no content) | `packages/server/test/routes/sites.test.ts :: C4: deleting a site removes it from the registry without touching the site itself` (structurally proven: no outbound fetch call exists anywhere in the DELETE handler's code path, not just untested); `packages/web/test/pages/HomePage.test.tsx :: C4: deleting a site (after confirming) removes it from the list`, `:: a delete that is not confirmed leaves the site in place`. Verified live: deleting the real registered site's entry left the real agent process running and answering `/v1/capabilities` unaffected |
| C5 | An unreachable site (network error) or an invalid/expired token (401 from capabilities or any later call) is surfaced clearly in the registry list, not a generic crash | `packages/server/test/sites/site-status.test.ts` (all four `SiteStatus` states against a real local `node:http` server, including a genuine timeout); `packages/web/test/sites/SiteStatusBadge.test.tsx` (all four states rendered). Verified live: a wrong token against the real running agent site produced `unauthorized`, and an unreachable port produced `unreachable`, both alongside a genuinely `ok` entry in the same list response |

Design notes:

- **C5's own wording is technically wrong about this repo, found by reading the actual code before designing, not assumed**: it says "401 from capabilities," but `GET /v1/capabilities` here (`src/routes/capabilities.ts`) is completely exempt from auth - no `preHandler`/`requireScope` at all - and returns 200 regardless of whether a token is present, garbage, or missing. It can never produce a 401. The admin's `checkSiteStatus` therefore makes two sequential calls, not one: `GET /v1/capabilities` (unauthenticated - proves reachability and that this is really a cms-agent instance) then `GET /v1/content` (token-scoped, requires the `content` scope, a cheap side-effect-free GET) - only the second can actually prove a stored token is valid. Confirmed `GET /v1/content` with no query params returns `200 []` safely on a site with zero content, not an error, before relying on it as the probe.
- **This is the first place the admin actually calls out to a registered site's API**, and Group A's `A4` static-analysis guard (no hardcoded site URL, written before any site-calling code existed) and Group B's `B` guard (every route requires auth or sits on a reasoned exemption) both kept passing with **zero new allowlist entries** - exactly what both tests' own comments anticipated. `checkSiteStatus` builds every outbound URL via `new URL(path, site.url)`, never a literal `http(s)://` string, and every sites route carries its own inline `requireAuth`.
- **The raw site token is never sent to the browser, in either direction** - not on list, not on register, not on rotate. `SiteListEntry` (the response DTO) is built by an explicit mapping function rather than spreading the `Site` record, so a future refactor can't accidentally start leaking it. Token rotation is a pure overwrite with no pre-fill - the old value never reached the browser to begin with, so there was never anything to look up or echo back.
- **Registration is never gated on reachability** - a site with a bad URL/token is still added to the registry (metadata only, per C4's own framing); it just shows an unreachable/unauthorized status from then on, the same as any other entry. A deliberate, reasoned call rather than inventing a rejection flow nothing in the checklist asked for.
- **Live status on every list request, not a cached snapshot** - confirmed directly with the user against the alternative (stored status + an explicit "recheck" action): simpler data model, directly satisfies C5's "surfaced clearly in the registry list" without a stale-status risk, and acceptable at this project's expected scale (one admin instance, a handful of registered sites) since total list latency is bounded by the single slowest site's timeout, not the count of sites (`Promise.all`).
- **The registry UI replaces `HomePage.tsx`'s placeholder content in place at `/`, not a new `/sites` route** - flagged rather than silently decided: there's no nav bar anywhere yet, and the placeholder text ("Nothing registered yet.") already read as intentional foreshadowing. A real nav/layout shell is deferred until Group D adds a second real page that needs to link back to the registry.
- **Verified against a genuinely running second process, not just a scaffold fixture**: a real site was produced via the agent repo's own `create-site` CLI, packed with `npm pack` and installed from the tarball (not a `file:`-to-source-directory link, which would silently defeat the point), and booted as a real separate OS process on its own port. The admin's registry then registered it, rotated its token to a wrong value and back (watching the live status flip `ok` -> `unauthorized` -> `ok`), and deleted it while confirming the agent process kept running and answering `/v1/capabilities` unaffected - proving C4's "removal never touches the site" against a real process, not just by reading the handler's code.

## Group D: content browsing

| # | Criterion | Proof |
|---|---|---|
| D1 | Selecting a registered site lists its content via `GET /v1/content`, showing at minimum path, type, and draft/live status per entry | `app-granite-cms-admin`: `packages/server/test/sites/site-content.test.ts` (all outcome states against a real local `node:http` server); `packages/server/test/routes/sites.test.ts :: D1: lists real content entries from a real fake site`; `packages/web/test/pages/ContentBrowserPage.test.tsx :: D1: lists content with path, type, and a derived live/draft status`. Verified live: a real second `create-site`-scaffolded agent instance's actual starter content (`pages/index.json`, `pages/404.json`, `menus/main.json`) listed correctly through the real UI, including the honest "Unknown" status label for `menus/main.json` (menus have no live/draft semantics, not a bug) |
| D2 | The list is filterable by type and draft status, matching what the agent's `GET /v1/content` already supports server-side | `packages/server/test/sites/site-content.test.ts :: forwards type, prefix, and draftStatus as query params`; `packages/server/test/routes/sites.test.ts :: D2: forwards type, prefix, and draftStatus query params to the site`; `packages/web/test/pages/ContentBrowserPage.test.tsx` (type and draftStatus filter changes re-request with the right query param, both live in the page's own URL via `useSearchParams`). Verified live: filtering the real registered site's content by `type=page` and by a nonexistent type both returned the real, correctly-filtered results |
| D3 | Selecting a content entry opens it in the editor (Group E), passing along whichever of live/draft actually exists for it | `packages/web/test/pages/ContentBrowserPage.test.tsx :: D3: each row links to the editor route with the path and a status hint in state`; `packages/web/test/pages/PlaceholderEditorPage.test.tsx` (renders from the URL alone with no state, renders the status hint when state is present, renders sensibly with no `path` query param at all). Verified live: clicking a real content row navigated to the editor showing the real site id/path/live status, and a direct hit on the editor URL with no navigation state (a hard refresh or bookmark) still rendered correctly rather than crashing or showing stale data |

Design notes:

- **D3's real scope, worked out before building anything**: Group E (the sidebar editor) doesn't exist yet, and its own E1 criterion ("opens a page for editing, reads its current content - draft if one exists, else live") makes clear the *editor* is what fetches/decides draft-vs-live, not Group D. So D3 is genuinely just correct navigation. But matching this project's own track record (every group ships something clickable, never a dead link) and CLAUDE.md's "no half-finished implementations," a minimal placeholder editor page was built at that route instead of leaving it to 404 until Group E - no fetch, no form, just path/site/known-status and an explicit "editing isn't built yet."
- **The backend content-list proxy does ONE call, not `checkSiteStatus`'s two.** `checkSiteStatus`'s capabilities-then-content sequence exists specifically because capabilities is auth-exempt and alone can't prove a token works - that matters for a health check. Listing content is calling the real resource actually wanted; that single call's own outcome (network failure, 401/403, non-2xx, malformed body, or success) already tells us everything.
- **Failures map to `502` with `{error, reason}` in the body, not a reused `401`.** `401` already means one specific thing everywhere else in this admin (the caller's *own* session is invalid, via `AuthError`/failed login) - reusing it for "the downstream site rejected our token" would be a real footgun the moment a global "on 401, log out" interceptor gets added later. `403`/`502` are unused anywhere in this server today (checked via grep, not assumed). `502` is semantically correct regardless of which of three ways the upstream failed, since this route genuinely is a gateway to another server - `reason`, not the HTTP status, is what the frontend branches on.
- **`fetch-site.ts` extracted as a narrow shared helper** (URL construction via `new URL(path, site.url)`, timeout wrapping, the network-failure try/catch only) once a second real caller needed the same shape - not a broad "site client" abstraction; each caller keeps its own interpretation of the response, since a 4-way health status and a 3-way content-fetch failure reason are different domain concepts. `checkSiteStatus`'s own existing test suite passed unchanged after the refactor, proving no behaviour change.
- **The content browser always makes its own live call, never gating on the registry's last-seen status** - Group C's own C5 principle ("live status on every list request, no cached snapshot") would quietly break one page over otherwise; a site can go unreachable/unauthorized between registration and a browse click.
- **A real test-isolation gap found by running the existing suite, not theorised**: `HomePage.test.tsx` rendered the component with no Router context, which broke the moment `HomePage` gained a `<Link>` for the new "Browse content" action. Fixed by wrapping every render call in a `MemoryRouter`, matching `App.test.tsx`'s already-established pattern.
- **No nav shell yet** - a trivial shared `BackToRegistryLink` component, not a layout route with persistent chrome. One link target, no multi-item navigation to speak of yet.

## Group E: sidebar editor, autosave, and conflict handling

| # | Criterion | Proof |
|---|---|---|
| E1 | Opening a page for editing reads its current content (draft if one exists, else live) and its `ETag`, via `GET /v1/drafts/:path` or `GET /v1/content/:path` | `app-granite-cms-admin`: `packages/server/test/sites/site-editor-content.test.ts` (draft-else-live, never calling live when draft succeeds, not-found, unreachable/unauthorized at either hop); `packages/server/test/routes/sites.test.ts :: E1` (both source cases via the real route, with the `ETag`/`X-Content-Source` headers). Verified live: read a real page from a real running agent instance, confirmed `x-content-source: live` with no draft, then `draft` once one existed |
| E2 | Editing a field and pausing triggers an autosave (`PUT /v1/drafts/:path`) with the `If-Match` header set from the last-known `ETag`, without an explicit save action from the user | `packages/server/test/sites/site-draft-save.test.ts :: E2, E3`; `packages/web/test/editor/useAutosaveDraft.test.ts :: E2`; `packages/web/test/pages/PageEditorPage.test.tsx :: E2, E3`. Verified live in a real browser: editing the textarea and pausing produced a real `PUT` with no explicit save button, status flipping dirty -> saving -> Saved, and the real file on disk changed |
| E3 | A successful autosave updates the locally-held `ETag` to the response's new value, so the next autosave's `If-Match` is always current | `useAutosaveDraft.test.ts :: E3` (a second edit after the first save succeeds, using the freshly-held etag) and the chained-save test (typing again while a save is in flight queues a follow-up with the fresh etag, no overlapping PUTs) |
| E4 | A `409` response (stale `If-Match`, another editor or tab saved first) surfaces a clear "this page changed since you opened it" state, per the build plan's own stated design, not a generic error toast | `packages/server/test/routes/sites.test.ts :: E4` (a stale If-Match forwarded as a real 409, not a generic error); `useAutosaveDraft.test.ts :: E4`; `PageEditorPage.test.tsx :: E4`. Verified live: a genuine second client (curl, standing in for another tab) saved first against a real running agent, and the open browser tab's own next autosave landed in the exact conflict state with the exact message, its own unsaved text preserved |
| E5 | From the conflict state, the user can reload to the latest version (discarding local unsaved changes) or view what changed, before choosing to proceed | `useAutosaveDraft.test.ts` (`reloadLatest` discards local content and re-fetches; `loadComparison` fetches the latest without touching authoritative state - viewing never silently resolves the conflict); `PageEditorPage.test.tsx :: E5` (both actions). Verified live: "View changes" showed the real server version and the real unsaved local version side by side (no diff library), and "Reload latest version" correctly replaced the textarea with the real current content |
| E6 | Two browser tabs editing the same page: the second to save gets the 409 path (E4/E5), the first's save is never silently lost | `packages/server/test/routes/sites.test.ts :: E6` - two concurrent `app.inject()` PUTs (unawaited `Promise.all`) against a real local fake site with an artificial PUT delay guaranteeing genuine overlap, confirming exactly one 200 and one 409, not a statistical near-certainty |

Design notes:

- **The read route does ONE fewer call than `checkSiteStatus` needs, and the write route reinvents nothing the site already got right.** `fetchSiteEditorContent` is two sequential calls (draft, falling back to live only on a literal 404), not `checkSiteStatus`'s capabilities-then-content dance - that dance exists specifically to prove token validity before trusting an auth-negative result, which doesn't apply here. A new shared `interpretSiteResponse` helper (not a richer per-operation union) handles the write route too: only "unreachable" and "401/403 remapped" need real interpretation - every other status (200, 404, 409, 428, 400) the site already produced correctly and is forwarded byte-for-byte.
- **401 is never reused for a downstream site failure.** In this codebase `401` means exactly one thing everywhere else - the caller's *own* admin session is invalid (`AuthError`/failed login). A downstream site's rejected token or conflict is `502`/`409`/`428` with a `{error, reason}` body instead, closing off a real footgun (a future global "on 401, log out" interceptor would otherwise misfire on a downstream failure).
- **`fetch-site.ts` was extended (method/headers/body), not bypassed**, so the write path still builds its URL through the one function that keeps `A4`'s hardcoded-URL guard passing. `site-status.ts`/`site-content.ts`'s full test suites (24 tests) passed unchanged after the extension, proving no behaviour change.
- **ETags are fully opaque pass-through strings, verified empirically, not trusted from the dependency list**: a quoted `ETag` round-trips through `fetch` + `interpretSiteResponse` with its quotes intact, settling whether undici or Fastify's own header handling could mangle it before any route was built against that assumption.
- **The autosave state machine is single-flight with a queued follow-up, not cancel-and-restart or overlapping saves** - refs (not just React state) back etag/content/in-flight/pending-save so the debounce timer and chained saves always read synchronously-current values. A `409` stops the loop entirely (not just the current attempt) until an explicit reload; malformed JSON is caught client-side so the backend never has to handle it.
- **`loadComparison` (E5) reuses the exact same draft-then-live read as the initial load, not a live-only fetch** - the far more common real conflict source is another tab's own *draft* autosave (E6's own scenario), and a live-only comparison would show the wrong thing for exactly that case.
- **No diff library**, consistent with this checklist's own still-open Group H question - "view what changed" is the server's latest text and the user's own unsaved text shown side by side, verified live to render both correctly.
- **A real fake-timers/RTL `waitFor` deadlock, found while testing, not theorised**: mixing Vitest's fake timers with `waitFor` hangs, since `waitFor` polls via the same timer mechanism the fake clock freezes. Fixed with an optional `debounceMs` parameter on the hook (defaults to 1000ms) and real timers with a tiny value in the hook's own tests; `PageEditorPage`'s own tests instead use an explicit longer `waitFor` timeout rather than threading test-only plumbing into the component.
- **A real shell-quoting bug, found in the verification script itself, not the product**: a `bash -c` string with an interpolated `ETag` value (which itself contains literal quote characters) silently produced a malformed `If-Match` header, making the wrong side of a simulated two-tab race fail. Fixed by calling `curl` with an argument array instead of a shell string, avoiding the quoting collision entirely - worth recording since it's exactly the kind of mistake that would misreport a real product bug as passing.

## Group F: live preview

| # | Criterion | Proof |
|---|---|---|
| F1 | The editor renders a live preview iframe pointed at the site's authenticated preview route for the page being edited | `app-granite-cms-admin`: `packages/server/test/sites/site-preview.test.ts`; `packages/server/test/routes/sites.test.ts :: F1, F3` (real fake-site tests for `GET /api/sites/:id/preview/*`); `packages/web/test/editor/PreviewFrame.test.tsx :: F1`; `packages/web/test/pages/PageEditorPage.test.tsx :: F1`. Verified live: opened a real page in the editor against a real running second agent instance and confirmed the iframe's `src` pointed at the admin's own same-origin proxy route, never the site directly |
| F2 | An unsaved edit reaches the preview without a full page reload of the admin app, via `postMessage` (or an equivalent mechanism decided at this session) | `PreviewFrame.test.tsx :: F2` (the src's cache-busting token changes only on a real completed autosave, `'saving' -> 'ready'`, not on initial load or conflict entry/exit). Verified live in a real browser: edited the textarea, let autosave fire, and watched the iframe visibly reload with the new content - confirmed via the real `<iframe>`'s `contentDocument.title` changing and a fresh `GET .../preview/...?t=1` request in the network log - while the admin app's own URL bar never changed (no `page.goto`, no route navigation) |
| F3 | The preview reflects the real draft state a publish would produce - not a client-side approximation - by actually hitting the agent's preview route, matching the build plan's own "editors see exactly what publish will produce" requirement | `site-preview.test.ts :: F1, F3` (a real rendered HTML page forwarded, status and content-type included, not re-rendered or transformed admin-side beyond the one `<base>` tag injection - see design notes). Verified live: `curl`'d the real agent's own `GET /v1/preview/*` directly and confirmed it matched what the admin's iframe showed - the draft title `"Changed by tab B (curl)"` (later a real theme's own content) appeared in both, styled and scripted correctly once the asset-resolution fix below was in place |
| F4 | A page with no draft (viewing live content only) still previews correctly, falling back to live per the agent's own preview-route behaviour | `sites.test.ts :: F4` (the site's own live-fallback HTML forwarded correctly, and its own 404 JSON forwarded verbatim when neither draft nor live exists - not swallowed or mistaken for a proxy failure). Verified live: opened `pages/404.json` (no draft) in the editor and confirmed the iframe rendered the real live "Page not found" content without error |

Design notes:

- **The agent gained one small, additive change: a `url` field on `GET /v1/content`.** The editor only ever held a content *file path* (`pages/about.json`), never the public URL the preview route needs (`/about`) - the conversion already existed agent-side (`pagePathToUrl`/`postPathToUrl`, previously only used internally by `move.ts` for redirect generation on rename) but had never been exposed via any route. Computed server-side by reusing those exact functions after stripping the `pages/`/`posts/` prefix, so the URL scheme stays single-sourced where it's actually implemented, rather than re-derived independently on the admin side. Menu entries have no public URL (`url: null`) - the preview component shows a plain fallback message instead of pointing an iframe at nothing.
- **`GET /v1/preview/*` already does its own draft-over-live overlay and its own 404** - unlike Group E's editor-content route, the admin's `site-preview.ts` needs only one call, not a draft-then-live sequence, and forwards whatever comes back with no interpretation of its own. This is what makes F3 true rather than merely claimed.
- **One narrow exception to "forwarded byte-for-byte", found via real dogfooding, not anticipated up front**: the rendered HTML's root-relative asset references (`href="/assets/style.css"`) are meant to resolve against the *site's* own origin, but once embedded in the admin's iframe the document was actually loaded from the admin's own proxy URL, so the browser resolved them against the admin's origin instead - silently hitting the admin's own unrelated `/assets/*` route (its SPA fallback) rather than 404ing loudly, so the failure looked like "no styling" rather than an obvious broken link. `site-preview.ts` now injects a single `<base href="{site.url}/">` tag into the `<head>` of any `text/html` response (a no-op if there's no `<head>` to attach to; untouched for the 404 JSON case). This only changes how the browser resolves resource URLs - it does not alter the page's actual rendered content or draft state, so F3's "not a client-side approximation" guarantee still holds for what the criterion is actually about.
- **The proxy forwards the real `Content-Type`, not a fixed one.** `interpretSiteResponse` (Group E's shared helper) doesn't carry content-type today, since its two existing callers (E1 read, E2 write) both fix their own and never needed the site's real one. Rather than widen a helper three routes depend on for this one new caller, `site-preview.ts` reads `content-type` directly off the raw `Response` at the one call site that needs it - headers access doesn't consume the body stream, so this is safe alongside `interpretSiteResponse`'s own `arrayBuffer()` read of the same result. Lower blast radius than touching shared surface area three routes already depend on.
- **F2 is a plain React-level src reassignment, not `window.postMessage`.** The previewed document is static server-rendered HTML, not a React app with its own JS that needs to talk back to the parent - there is no cross-frame data to relay, only a "reload yourself" signal, which changing the iframe's `src` (with a cache-busting token) already accomplishes natively without touching the admin SPA's own router or state. The refresh token only bumps on the specific `'saving' -> 'ready'` transition, tracked via a `ref` local to the new `PreviewFrame` component - `useAutosaveDraft` itself stays untouched, since it's deliberately UI-agnostic per Group E's own I6 note and touching it would re-open all of Group E's test surface for no functional gain. A `'loading' -> 'ready'` transition (the "Reload latest version" conflict-recovery path) needs no equivalent handling: `PageEditorPage` unconditionally renders a loading placeholder in place of the whole editor (including `PreviewFrame`) whenever status is `'loading'`, so the preview frame unmounts and remounts fresh as a genuinely new iframe element on recovery, rather than needing a second explicit refresh trigger.
- **`url` threads through as a second query parameter (`&url=...`), not router state**, for the same reason `path` already does: a bookmarked or directly-reloaded editor URL must keep working, and route state doesn't survive that. `path` stays the sole key for the existing content-read/draft-save routes - `url` is purely additive, consumed only by `PreviewFrame`.
- **Verified live end-to-end** against a real second `create-site`-scaffolded agent instance (rebuilt and repacked after the `url`-field change, reinstalled from a fresh `file:` tarball): registered the site, opened the homepage (a real draft differing from live) and confirmed the iframe showed draft content; edited the textarea and watched the iframe refresh automatically after the real autosave landed, with the admin app's own URL never changing; opened a page with no draft and confirmed the real live-fallback content rendered; opened a menu entry and confirmed the "No live preview available for this content type" fallback, no broken iframe.

## Group G: publish and discard controls

| # | Criterion | Proof |
|---|---|---|
| G1 | An explicit "publish" action prompts for (or auto-generates, per build plan wording) a commit message before calling `POST /v1/publish` | `app-granite-cms-admin`: `packages/server/test/sites/site-publish.test.ts`; `packages/server/test/routes/sites.test.ts :: G1` (the fake site receives the real batch shape, with the logged-in admin's own name/email as author, never something the caller supplied; a blank message is rejected with 400 before the site is ever called). `packages/web/test/pages/PageEditorPage.test.tsx :: G1`. Verified live against the persistent `fixtures/demo-site` instance: publishing the fixture's own example draft produced a real git commit in `.demo-runtime` with the typed message and `admin <admin@admin.local>` as author |
| G2 | A successful publish clears the local draft state in the editor and reflects the page as now-live | `sites.test.ts :: G1` (content list's `hasDraft` flips to `false` after publish, proven through the real content route, not the fake site's internal state); `PageEditorPage.test.tsx :: G1` (`source` flips from `draft` to `live`). Verified live: the draft file was gone from `.demo-runtime/content/drafts/` after publish, and the content list showed `hasDraft: false` |
| G3 | An explicit "discard" action calls `DELETE /v1/drafts/:path` and returns the editor to the live version, with a confirmation step first (a destructive, irreversible-in-the-UI action) | `site-draft-discard.test.ts` (idempotent - 204 whether or not a draft existed); `sites.test.ts :: G3`; `PageEditorPage.test.tsx :: G3` (`window.confirm` declined makes no call at all; confirmed, it calls discard and reloads to the live version). Verified live: saved a real draft on the 404 page, discarded it via `DELETE`, confirmed the content list showed `hasDraft: false` again with no new git commit (discard never commits, matching the agent's own drafts-are-never-git-tracked design) |
| G4 | An "unpublish" action calls `POST /v1/unpublish/:path` and is reflected in the content list/editor as no longer live | `site-unpublish.test.ts`; `sites.test.ts :: G4` (`published:false` set on the live file in place, file never deleted). No admin UI proof any more - see the 2026-08-04 design note below; the route (agent and admin server, both still fully proven and unchanged) is the only part of G4 this criterion now covers |
| G5 | A publish or unpublish that fails (network error, validation rejection from the agent) leaves the draft state untouched in the UI, never showing a false success | `PageEditorPage.test.tsx :: "G5: a failed publish leaves the draft state untouched and shows an inline error"` (a mapped failure response leaves the textarea content and status unchanged, shows an inline error) - this falls out of the design rather than being bolted on: `reloadLatest()` (the only thing that ever mutates `useAutosaveDraft`'s state for this feature) is only ever reached past a successful `await`, so a thrown error can't touch it. The unpublish half of this row has no UI-level proof any more, for the same reason as G4 above; `site-unpublish.ts`'s own error-mapping (unreachable/unauthorized/error) remains proven at the API-client layer (`site-unpublish.test.ts`) |

Design notes:

- **The commit author is never typed by the caller.** Publish/unpublish both need a real git commit author (`{name, email}`), which the admin's `AdminUser` model had no room for. Decided with the project owner: stored on the admin account itself, set once at bootstrap (`ADMIN_BOOTSTRAP_NAME`/`ADMIN_BOOTSTRAP_EMAIL` env vars, defaulting to the username and a synthetic `id@admin.local` address), not typed fresh per publish and not derived per-request. `require-auth.ts`'s `currentUser` widened to carry it through to route handlers, which build the `CommitAuthor` server-side - the browser only ever supplies a path and a commit message. A quiet backfill runs at every boot for any pre-existing account (Groups A-F) missing these fields, so upgrading an existing deployment never leaves an invalid record on disk.
- **Publish is single-page from the browser's point of view, even though the agent's own route is a batch API.** The browser sends `{path, message}`; the admin route wraps it into the agent's real `{paths: [path], message, author}` shape server-side. This keeps `site-publish.ts` a faithful 1:1 mirror of the real API (reusable if multi-select publish is ever added later) while "this UI is single-page only" stays a route-layer decision, the same separation `parseContentFilters`/`fetchSiteContent` already keep in Group D.
- **Three separate backend modules** (`site-publish.ts`, `site-draft-discard.ts`, `site-unpublish.ts`), matching this codebase's existing one-file-per-site-operation granularity (`site-draft-save.ts`, `site-preview.ts`) rather than combining them.
- **No new frontend error class or path-encoding helper.** `site-editor.ts`'s existing `SiteEditorError`/`SiteEditorErrorReason` already covered every reason these three calls need; its private `encodePathSegments`/`reasonFromResponse` helpers were simply exported (visibility widening only) and reused directly from the new `site-publishing.ts`.
- **`useAutosaveDraft` was not modified.** Publish/discard/unpublish are a separate concern layered beside it in `PageEditorPage.tsx` (local `actionBusy`/`actionError` state), calling the hook's own `reloadLatest()` only after a successful action - this is what makes G5 true by construction, not bolted on afterwards.
- **Publish is disabled while the editor's own status is `dirty` or `saving`.** `POST /v1/publish` publishes whatever is already saved as a draft on disk, not the browser's current textarea content - publishing mid-edit, before the debounce fires, would silently ship stale content. Only enabled once a save has actually landed (`status === 'ready'`).
- **Discard reuses `HomePage.tsx`'s existing `window.confirm` precedent exactly** (Group C's site-removal confirmation) rather than inventing a new confirmation pattern. Publish and unpublish each use `window.prompt` for the commit message; no second confirm on unpublish beyond that, since it's reversible by republishing and the checklist doesn't call for one.
- **`ContentBrowserPage.tsx` needed no changes at all** - it already performs a fresh, uncached fetch on every mount (Group C/D's "always live" principle), so navigating back after a publish/discard/unpublish already shows current state with zero new code.
- **2026-08-04: the Unpublish button was removed from `PageEditorPage.tsx`'s header entirely, not relocated yet.** Raised directly by the project owner while cleaning up the editor's top bar: unpublish is a page-level action best reached from the page tree (content browser), not the single-page editor, but where exactly it resurfaces there is its own design question, deliberately deferred rather than decided as a side effect of a cleanup pass. `POST /v1/unpublish/:path` and the admin's own `unpublishSite`/`site-unpublish.ts` route are untouched and fully proven (G4's own table row) - only the editor's UI entry point is gone, along with its three `PageEditorPage.test.tsx` UI tests and the now-dead `unpublishSitePage` caller in `site-publishing.ts` (the exported client function itself was kept, unused, since a page-tree action will need the identical call). This also prompted a second, related change to the same header: the "Editor / Site: ... Path: ... Source: ..." debug line and the plain "Saved" status text were removed, and Save/Discard draft now only render once there is something to act on (`source === 'draft'` or the autosave status is not `'ready'`) instead of sitting there permanently, with Save styled as the primary (blue) action to the right of Discard - proven by `PageEditorPage.test.tsx`'s updated E1/E2/E3/E5/G1 assertions (button presence/absence and `disabled` state in place of the removed status text) and verified live against `fixtures/demo-site`: the header shows nothing extra on a page with no draft and no in-progress edit, and Save/Discard appear on the very next keystroke, before the autosave debounce fires.

## Group H: page history

| # | Criterion | Proof |
|---|---|---|
| H1 | A history view for a page lists its commits via `GET /v1/git/log`, scoped to that page's path | `app-granite-cms-admin`: `packages/server/test/sites/site-history.test.ts`; `packages/server/test/routes/sites.test.ts :: H1`; `packages/web/test/pages/PageHistoryPage.test.tsx :: H1`. Verified live against the persistent `fixtures/demo-site` fixture: published a page twice and reverted it once (three real commits plus the fixture's own initial commit), and the history view listed all four with correct hash/author/date/message |
| H2 | Draft checkpoint commits (flagged `isCheckpoint` by the agent) are hidden by default, with an explicit toggle to reveal them | `PageHistoryPage.test.tsx :: H2` (checkpoint commits hidden by default; toggle reveals them via a pure client-side re-filter, proven by asserting no extra fetch fires on toggle). Not independently re-verified live with a real checkpoint commit in this session (the background checkpoint job runs on its own interval, which didn't fire within the live-verification window) - the `isCheckpoint` field itself is already verified live end-to-end for every commit returned (H1's proof), and the exact-match-only detection (`message === 'chore: draft checkpoint'`) is the agent repo's own, already-proven Phase 2 behaviour |
| H3 | Selecting two revisions (or a revision against current) shows a real diff, via `GET /v1/git/show/:ref/:path` against each side, using the tool chosen at open question 5 | `site-revision.test.ts` (literal ref `HEAD` round-trips; `invalid-ref`/`not-found-at-ref` kept distinct); `sites.test.ts :: H3`; `PageDiffView.test.tsx`; `PageHistoryPage.test.tsx :: H3` (1 selected -> diff against HEAD; 2 selected -> ordered old->new regardless of click order; a 3rd click while 2 are selected is a no-op). Verified live: diffed two of the real commits above and got a genuine jsdiff line diff with real added/removed lines; diffing a commit against current (HEAD) also returned the correct live content |
| H4 | One-click revert calls `POST /v1/git/revert` and the page reflects the reverted content afterwards, as a new commit - history is never rewritten, matching the agent's own guarantee | `site-revert.test.ts`; `sites.test.ts :: H4` (sends the logged-in admin's own name/email as author); `PageHistoryPage.test.tsx :: H4`. Verified live: reverted the demo page to its first real commit - a genuine new commit appeared at the top of `git log` (all four prior commits still present, history never rewritten) and the live content was confirmed byte-for-byte reverted |

Design notes:

- **A load-bearing detail found by reading the agent's own source, not assumed**: `GET /v1/git/log`, `GET /v1/git/show/:ref/:path`, and `POST /v1/git/revert` all resolve paths relative to `siteRoot`, not `contentRoot` like every other route this admin calls. Every new backend module prefixes the admin's usual bare path (`pages/about.json`) with `content/` internally before calling the agent - confirmed directly against `git-history.ts`/`git-revert.ts` and their own tests (which themselves always pass `content/pages/...`), not just inferred.
- **"Compare against current" is just `ref='HEAD'` reused through the exact same show route** - confirmed via a real agent-side test (`git-history.test.ts`) that the literal string `"HEAD"` is a valid, working ref. No separate live-content endpoint was needed for this.
- **`invalid-ref` and `not-found-at-ref` are kept genuinely distinct at every layer** (agent status code, admin site-* module outcome, admin route response, all the way down to the frontend's error message text), matching the agent's own deliberate non-collapse of these two failure reasons - only the final frontend error-reason enum collapses them into one bucket, a conscious, narrow trade-off since the human-readable message stays specific regardless.
- **jsdiff, plain unified diff, no merge-request UI** - decided directly with the project owner after a real discussion about audience fit: the admin's users are content marketers, not developers, and the editor itself is still raw-JSON-only until a future Group I brings real per-field editing - so a schema-aware/field-level diff would need foundations that don't exist yet, and a plain line diff (computed client-side, zero server-side dependency) is judged the right scope for now. It still catches the two things this audience actually needs from it: structural surprises (an accidentally-deleted section) and confidence before reverting.
- **Revert never touches an open draft** (drafts are never git-tracked) - decided to show a passive warning note on the history page rather than adding an active draft-existence check, keeping this group's scope tight rather than adding a new cross-check the checklist didn't call for.
- **Flat, top-level route paths** (`/:id/history/*`, `/:id/revision/:ref/*`, `/:id/revert`), not nested under a shared prefix - a real page path could otherwise collide with a static route segment and be silently misrouted, since Fastify's router prefers a more specific static match over a wildcard sibling at the same prefix.

## Group I: section and block editing

| # | Criterion | Proof |
|---|---|---|
| I1 | A page's sections render as an editable, reorderable list, matching the Shopify-customiser interaction model named in the build plan | `app-granite-cms-admin`: `packages/web/src/sections/SectionList.tsx` (move-up/move-down, bounds-checked); `SectionList.test.tsx :: I1`; `PageSectionsEditor.test.tsx` (reorder reaches `setContent`); `PageEditorPage.test.tsx :: "Group I: sections content defaults to the structured editor..."`. Verified live against `fixtures/demo-site`: reordered the real homepage's `sec-hero`/`sec-social-proof` sections and confirmed the draft file's array order actually changed on disk |
| I2 | Adding a section presents only section types available in the site's active theme (read from the theme's schema data, not a hardcoded list) | `app-granite-cms`: `src/routes/theme.ts` (new `GET /v1/theme/schemas`); `test/routes/theme.test.ts`; `test/services/theme-schemas.test.ts`. `app-granite-cms-admin`: `packages/server/src/sites/site-theme-schemas.ts` + `site-theme-schemas.test.ts`; `packages/web/src/api/site-theme-schemas.ts`; `SectionList.test.tsx :: I2` / `BlockList.test.tsx :: I2` (add-type `<select>` options sourced from the fetched schemas, not hardcoded). Verified live: `GET /v1/theme/schemas` against the real demo theme returned real section/block schemas for every `.liquid` file, and the add-section/add-block dropdowns in the browser showed exactly those real type names |
| I3 | A section's settings form is generated from its JSON Schema (from the theme), matching field types (string, number, boolean, enum) to appropriate inputs | `packages/web/src/sections/SchemaField.tsx` (enum -> `<select>`, string -> text, integer/number -> number, boolean -> checkbox, unrecognised -> raw-JSON fallback); `SchemaField.test.tsx`; `SectionSettingsForm.tsx` + `.test.tsx`. Verified live: the real `button` block's `style` enum (`primary`/`secondary`/`on-dark`) rendered as a real `<select>` with exactly those three options, and changing it saved the new value to the draft on disk. The integer/number widget has no live-fixture counterpart (no real theme file in this project declares one today) - covered only by `SchemaField.test.tsx`'s synthetic schema case |
| I4 | A block can be added to, removed from, and reordered within a section that supports blocks; a section that does not support blocks shows no block controls | `app-granite-cms`: `acceptsBlocks` computed in `src/services/theme-schemas.ts` from `parsed.markup.includes('blocksHtml')`, added to `ThemeSchemas` (`src/services/validation.ts`). `app-granite-cms-admin`: `packages/web/src/sections/BlockList.tsx` + `.test.tsx` (I2/I4/I5/I6); `SectionList.tsx`'s own `acceptsBlocks`-gated render of `BlockList` (`SectionList.test.tsx :: I4`, both the shows-controls and shows-no-controls cases). Verified live: added a real `button` block to the homepage's `hero` section (which accepts blocks) and removed it again, confirming the draft's block array on disk grew then shrank back to the original two blocks |
| I5 | An invalid settings value (rejected by the agent's schema validation on save) is surfaced against the specific field, not just a generic save failure | `app-granite-cms`: `DraftError.errors: ValidationError[]` (`src/services/drafts.ts`), forwarded by `src/routes/drafts.ts`'s 400 response; `test/services/drafts.test.ts` / `test/routes/drafts.test.ts` (`Group I` cases). `app-granite-cms-admin`: `site-draft-save.ts`'s `invalid.errors` (defensive decode, degrades to `[]` against an un-upgraded agent) + tests; `site-editor.ts`'s `SiteEditorError.validationErrors` + `reasonFromResponse`; `useAutosaveDraft.ts`'s new `validationErrors` field + test; `PageSectionsEditor.tsx`'s `buildFieldErrorMap` (flat `/sections/N/settings/key` and nested `/sections/N/blocks/M/settings/key` paths re-keyed by instance id) + `PageSectionsEditor.test.tsx` (a section-level error and a nested-block-level error each land on the right field, not the other's). Verified live twice: (a) added a block with empty required settings and saw both `must have required property 'label'` and `...'url'` rendered against that block's own fields, not a single generic message; (b) deliberately set a real button's `style` to an out-of-enum value via the raw-JSON fallback and saw `must be equal to one of the allowed values` against that exact field once switched back to the structured view |
| I6 | Reordering sections/blocks and editing settings all flow through the same autosave/`ETag`/conflict path Group E already built - no second, parallel save mechanism | `useAutosaveDraft.ts` gained exactly one additive return field (`validationErrors`), set only inside the pre-existing `attemptSave` catch branch - zero change to the debounce/single-flight/conflict state machine; `PageSectionsEditor.tsx` drives it purely via `JSON.parse`/mutate-a-clone/`JSON.stringify`/`setContent(...)`, the same contract every prior structured control uses. `BlockList.test.tsx :: I6`, `SectionList.test.tsx :: I6`, `PageSectionsEditor.test.tsx` (reorder/add/remove/settings edits all call the same `setContent`). Verified live: every structured edit in this session (reorder, enum change, add/remove block) went through the identical dirty -> saving -> ready status cycle already proven in Group E, and a genuine external draft write (bypassing the admin, direct `PUT /v1/drafts/...` against the real agent) while the structured editor was open surfaced "This page changed since you opened it.", with "View changes"/"Reload latest version" both working exactly as Group E's own tests describe |

Design notes:

- **Unlike every prior group, this one needed new agent-repo API surface, not just admin-side UI against an already-existing API** - confirmed by reading source, not assumed: `themeSchemas` was loaded once at boot and used only internally for write-time validation, never returned over the wire; validation-failure 400 responses had the real ajv errors JSON-stringified inside a message string rather than as a structured field; and whether a section/block type accepts nested blocks was expressed only in Liquid markup convention (does the template loop `blocksHtml`), never in the schema JSON at all. All three gaps were small, additive, and confirmed with the project owner before any admin-side code was written.
- **Block-type acceptance (`acceptsBlocks`) is computed from a markup substring check, not declared in schema** - `parseThemeComponentFile` already produced a `markup` field (the file with its `{% schema %}` block stripped) that was computed and then discarded; `markup.includes('blocksHtml')` is the entire fix. This is a real, if slightly informal, signal: it is exactly the same thing the Liquid renderer itself relies on to decide whether to loop `block.blocks`, so it cannot drift from the real rendering behaviour the way a separately-declared schema flag could.
- **No schema restricts which block types are valid under which section type** - confirmed via `instance.schema.json` (`blocks` is a fully generic array of `#`-refs) and `validateInstance`'s own code. `BlockList`'s add-block dropdown therefore offers every block type the theme declares, globally, under every blocks-accepting section - matching the real, confirmed absence of a schema-level restriction, not an oversight. A theme author who wants a `logo-mark` block to only make sense under `social-proof` enforces that today purely by what the section's own Liquid template chooses to render, exactly as block-acceptance itself already works.
- **Reordering is move-up/move-down buttons, not drag-and-drop** - zero new dependency in either repo, matching this project's consistent "simplest mechanism that works" preference (the same reasoning that chose plain jsdiff over a diff-viewer library in Group H). Real drag-and-drop remains a pure future enhancement layered on the same underlying array-splice reorder logic.
- **The structured editor is the default view; raw JSON is a fallback tab, forced automatically (not just offered) whenever the current content has no `sections` array** - a menu, or any content shape the structured editor cannot represent, has its "Sections" tab disabled and falls straight back to raw. Every existing `PageEditorPage.test.tsx` fixture happens to be shaped that way, so this fallback design meant zero rewrite of any pre-Group-I test assertion - a nicer outcome than the plan anticipated (which expected a mechanical rewrite of every `getByLabelText('Content')` call site).
- **A freshly-added section/block starts with empty `settings: {}`** and, if the real schema has required fields (as most do), is genuinely invalid until the user fills them in - confirmed live (adding a `button` block immediately shows `must have required property 'label'`/`'url'` against those two fields). This was a deliberate non-decision: pre-filling required fields with schema defaults or empty-string placeholders was judged out of scope for I1-I6, which only require that an invalid value be surfaced against the right field, not that new instances start valid.
- **`useAutosaveDraft.ts` and `site-editor.ts`, kept untouched by every group since E, each got exactly one additive field** (`validationErrors` / `SiteEditorError.validationErrors`) - both set only inside pre-existing catch branches, with no change to the debounce/single-flight/conflict control flow. This satisfies I6 by construction rather than by discipline alone: there was never a second code path capable of writing a draft.
- **Live verification deliberately included a genuine conflict scenario**, not just the happy path: a real `PUT /v1/drafts/pages/index.json` sent directly to the agent (bypassing the admin entirely) while the structured editor had the page open reproduced Group E's own E4/E5 scenario exactly, now driving the new UI - confirming I6's "same path" claim is actually true under a real race, not just true by code inspection.

## Group J: page name and slug

Not part of the original build plan - a scope addition requested directly by the project owner mid-Phase-3 (WordPress-style: a page's admin-facing "name" is distinct from its rendered `<title>`, and its slug can be renamed independently, without an automatic redirect). Confirmed against `docs/cms-build-plan.md`/every phase checklist before starting: no prior group anticipated a page-level name or slug field, and `move.ts`'s existing redirect-on-move was itself an established, unconditional behaviour, not a stub - both were real, deliberate departures from what existed, not an oversight being filled in.

| # | Criterion | Proof |
|---|---|---|
| J1 | A page carries a "name" field distinct from "title" - the schema requires it, with a migration defaulting existing content's name to its existing title | `app-granite-cms`: `src/schemas/page.schema.json` (`name` added to `required`/`properties`); `src/migrations/index.ts`'s `migrateV4ToV5` (`CURRENT_SCHEMA_VERSION` 4 -> 5); `test/migrations/index.test.ts` (migration output + full v1->current chain still validates). `content-read.ts`'s `listContent` applies the identical title-fallback defensively at read time too, since migrations aren't wired to run automatically - `content-read.test.ts`'s two new cases (`name` present; `name` absent, falls back to `title`) |
| J2 | The page tree (`app-granite-cms-admin`) displays and sorts by each page's "name", not its "title" | `packages/web/src/pages/pageTree.ts`'s `sortTree`; `ContentBrowserPage.tsx`'s `PageTreeRow` (display) and `matchesSearch` (search now also matches name); `pageTree.test.ts` / `ContentBrowserPage.test.tsx`'s dedicated "sorts by name even when it differs from title" and "the tree displays each page's name, not its title" cases. `ContentListEntry` widened end to end (agent -> `packages/server/src/sites/site-content.ts` -> `packages/web/src/api/site-content.ts`), with the admin server itself falling back to title if a connected site's agent hasn't been upgraded yet (`site-content.test.ts`) - the admin and each site deploy independently, so this could not assume every registered site is running the new agent build |
| J3 | A page's slug (URL) can be renamed independently of its name, via the agent's existing move operation - not a new field, since the URL has always been derived from the file path | `app-granite-cms-admin`: `packages/server/src/sites/site-move.ts` (new `POST /:id/move` proxy, author from session not client body, matching publish/revert precedent); `packages/web/src/api/site-publishing.ts`'s `moveSitePage`; `PageMetadataPanel.tsx`'s Slug field (live-follows a slugified Name once Name has changed this session, until hand-edited, matching WordPress's own permalink editor) + "Update slug" action. `site-move.test.ts`, `sites.test.ts`'s new move-route cases, `PageMetadataPanel.test.tsx`'s slug-follow/apply/error cases. Verified live: renamed a real demo page's slug from `/about` to `/about-the-company` - the URL changed, the preview repointed, and a subtree with real children (`about/team.json`, `about/careers.json`) moved with it, still correctly nested in the tree afterwards |
| J4 | Renaming a slug does not automatically create a redirect - unlike a real WordPress permalink change, which also doesn't | `app-granite-cms`: `movePage`/`prepareMovePage`/`POST /v1/content/move` gained an optional `createRedirect` option (default `true`, so every other/future caller is unaffected) - `move.test.ts`'s three new cases (false skips the new redirect; default still creates one; false still clears a stale redirect that already pointed at the destination, since that's cleanup, not creation) and `content.test.ts`'s two new route-level cases. `site-move.ts` always sends `createRedirect: false` - not a passthrough option, a decided default, since the only caller is this rename feature. Verified live: after the rename above, `GET /about` (the old slug) returned a real 404, not a redirect, and `redirects.json` had no entry for it |
| J5 | Renaming is unavailable while a draft is pending, rather than silently orphaning it | `move.ts` only ever moves the live file, never the drafts tree (confirmed by reading source, not assumed) - a pending draft at the old path would become invisible/unreachable once the live file moved. `PageEditorPage.tsx` passes its own existing `hasPendingChanges` (Group G's own flag, already gating the Save/Discard footer) straight through as `PageMetadataPanel`'s `renameDisabled`, rather than inventing a second dirty-check. `PageMetadataPanel.test.tsx`'s `renameDisabled` case (Slug field and Update slug button both disabled, with an explanation shown). Verified live: editing Name (a real draft-modifying field) immediately disabled Update slug with "Save or discard your changes before changing the URL." - saving the draft then re-enabled it |

Design notes:

- **The redirect-creation question was asked and answered directly with the project owner before this was built**, not assumed - the instinct ("we shouldn't auto-create a redirect") was checked against what `movePage` actually does today (always creates one, unconditionally, with no existing opt-out) and against real WordPress's own behaviour (core WordPress does *not* auto-redirect a changed permalink either - a well-known gap plugins like Redirection exist to patch), before deciding `createRedirect: false` was the right default for this feature specifically, not a hidden bug fix to `move.ts`'s existing behaviour for whoever else calls it.
- **A slug is not a new stored field** - it was, and remains, purely the page's file path (`pages/about.json` -> `/about`), exactly as `pagePathToUrl`/`urlToPagePath` already defined it. "Renaming a slug" is real file move, real commit, real git history - the same operation the build plan's own Phase 2 groundwork (`move.ts`) already proved out for a different original purpose (hierarchical URL restructuring), reused here rather than duplicated.
- **The Name field and the Slug field are deliberately two different kinds of write** - Name is ordinary content, flowing through the exact same autosave/draft/publish path every other field on this panel already uses; Slug is a structural, immediately-committing operation with its own explicit "Update slug" action, never silently triggered by typing. Conflating the two - e.g. auto-applying a slug rename on every keystroke - would mean a real git commit (and a real live URL change) for every character typed, which was never the intent even before the pending-draft safety question came up.
- **`key={path}` was added to `PageMetadataPanel`'s own mount in `PageEditorPage.tsx`** so that navigating to a different page (including the query-param-only navigation a successful rename itself performs) fully remounts the panel rather than carrying over stale local state (the slug-follow state, and incidentally the pre-existing placeholder description/author/publish-date fields too) - a small, targeted fix for what this feature specifically needed, not a broader refactor of those other placeholder fields' own known limitations.
- **The slug auto-follow only activates once Name changes from the value it held when the panel mounted**, not against whatever name a page already had on open - without this distinction, simply opening an existing page whose name and slug happen to differ would immediately show a "changed" slug and a wrongly-enabled Update slug button, as if a rename were already pending. Caught by a live-verification run before being caught by a written test, then a regression test (`PageMetadataPanel.test.tsx`'s "Slug starts at the current path's own stem, not derived from Name" case) was added to lock it in.

## Group K: block-type restrictions per section/block

Not part of the original build plan - a scope addition requested directly by the project owner mid-Phase-3. Group I's own design notes (line 198 above) named the absence of this restriction explicitly as a confirmed, deliberate current-state gap, not an oversight: "No schema restricts which block types are valid under which section type... A theme author who wants a `logo-mark` block to only make sense under `social-proof` enforces that today purely by what the section's own Liquid template chooses to render." This group closes that gap.

| # | Criterion | Proof |
|---|---|---|
| K1 | A theme author can restrict which block types are valid under a section/block type via an optional `allowedBlocks` array in its `{% schema %}` JSON; absent means unrestricted | `app-granite-cms`: `docs/theme-authoring-guide.md`'s "Restricting which block types are allowed" section; `src/services/validation.ts`'s `allowedBlockTypesOf` |
| K2 | The agent rejects a block instance whose type is not in its parent's declared `allowedBlocks`, as a structured error at `/blocks/N/type` | `test/services/validation.instance.test.ts`'s three new K2 cases: an allowed type passes, a disallowed type fails with `keyword: 'disallowedBlockType'` at the right path, and a parent declaring no `allowedBlocks` stays unrestricted (regression) |
| K3 | `GET /v1/theme/schemas` continues to return each type's schema verbatim, including `allowedBlocks`, with zero reshaping | `test/services/theme-schemas.test.ts` (loader preserves the extra key verbatim); `test/routes/theme.test.ts` (the route round-trips it); `test/renderer/theme-templates.test.ts` (existing fixture-schema assertion updated to include it) |
| K4 | The admin's "Add Block" menu - both a section's top-level blocks and a block's own nested blocks - only offers types allowed under that specific parent instance | `app-granite-cms-admin`: `packages/web/src/sections/instance-types.ts`'s `allowedBlockTypes`; `SectionList.tsx`/`BlockList.tsx` wiring; `SectionList.test.tsx`/`BlockList.test.tsx`'s new cases |
| K5 | An agent-rejected disallowed block type surfaces as a field-level error via the existing I5 error-plumbing, with zero new admin-side error-handling code | Verified live: added a `feature-card` block directly under the demo site's `social-proof` section via a raw `PUT /v1/drafts/pages/index.json` (bypassing the admin), confirmed the 400 response's `disallowedBlockType` error at `/sections/N/blocks/M/type`, then reloaded that page in the admin's structured editor and confirmed the error rendered against the right block instance through `PageSectionsEditor.tsx`'s existing `buildFieldErrorMap`, unmodified by this group |

Design notes:

- **`allowedBlocks` is declared on the parent type, not "which parents am I allowed under" on the child** - a block can legitimately have more than one valid parent. Confirmed against the real demo theme (`app-granite-cms-admin/fixtures/demo-site`): the `button` block is used under both `hero` and `cta-banner`. Each qualifying parent declares its own `allowedBlocks` independently - a deliberate, accepted small redundancy, mirroring how Shopify's own per-section `blocks` declaration works, not a flaw.
- **No new interface or plumbing was needed to carry the field through either repo** - `parseThemeComponentFile` already returned the whole schema JSON verbatim as an opaque object, `theme-schemas.ts` already stored it verbatim, and `GET /v1/theme/schemas` already serialised it verbatim. `allowedBlocks` rides through the entire pipeline for free, exactly as the pre-existing `title` keyword already does (read as plain metadata outside of Ajv, in the admin's `schemaTitle()`). This is also why cross-repo backward compatibility needed no defensive code anywhere: an un-upgraded agent simply omits the key inside its schema blobs, indistinguishable from any theme file that predates this feature.
- **Enforcement is entirely agent-side; the admin's menu filtering is a UX nicety, not the security boundary** - consistent with the established I5/I6 pattern of trusting the agent's own structured `ValidationError`s rather than duplicating validation logic in the browser. Already-saved blocks of a now-restricted-out type still render and remain editable in the admin - only the Add-Block menu is filtered; a theme author's later policy change never silently corrupts existing content.
- **The demo theme's `hero`, `social-proof`, and `cta-banner` sections were given real `allowedBlocks` declarations** (`["button"]`, `["logo-mark"]`, `["button"]` respectively) matching every real demo page's existing block usage exactly, confirmed by reading `content/pages/index.json` before making the change - every other demo section was deliberately left undeclared, so restricted and unrestricted sections exist side by side in one real theme for K5's live verification. (Every remaining demo section subsequently got its own `allowedBlocks` too, in follow-up work not tracked as a separate checklist group - the whole demo theme is now fully restricted, one block type per section.)

## Group L: required settings fields must declare a default

Another project-owner-requested addition, closing a second gap Group I's own design notes named explicitly (the note directly above K1 in this document, restated here for context): "a freshly-added section/block starts with empty `settings: {}` and, if the real schema has required fields (as most do), is genuinely invalid until the user fills them in... pre-filling required fields with schema defaults... was judged out of scope for I1-I6." This group closes that gap: every required settings property in a section/block schema must now declare a `"default"`, checked at theme-load time, and the admin uses those defaults to pre-fill a newly-added instance.

| # | Criterion | Proof |
|---|---|---|
| L1 | A theme author must declare a `"default"` for every property listed in a section/block schema's `"required"` array, and that default must itself satisfy the property's own constraints (not merely be present) | `app-granite-cms`: `docs/theme-authoring-guide.md`'s new paragraph on the settings-schema section; `src/services/validation.ts`'s `requiredFieldsHaveValidDefaults`; `test/services/validation.schema-defaults.test.ts`'s eight cases (missing default, default missing from `properties` entirely, an invalid default against `minLength`/`enum`/array `minItems`, and the no-`required`/empty-`required` trivial-pass cases) |
| L2 | A section/block type that violates L1 is silently excluded from the theme's registered schemas, the same way a malformed `{% schema %}` block already is - not a boot failure | `theme-schemas.ts`'s `loadTypeSchemas`, one `continue` alongside the existing malformed-schema skip; `test/services/theme-schemas.test.ts :: L2` |
| L3 | The admin pre-fills a newly-added section's or block's `settings` from whatever `"default"` values its schema declares (not required-specific - any property with a declared default gets pre-filled), instead of leaving it `{}` | `app-granite-cms-admin`: `packages/web/src/sections/instance-types.ts`'s `buildDefaultSettings`; `SectionList.tsx`'s `addSection`, `BlockList.tsx`'s `addBlock`; `instance-types.test.ts`, `SectionList.test.tsx`, `BlockList.test.tsx` |

Design notes:

- **Enforcing this rule immediately obsoleted every required field in both repos' existing theme fixtures** - a real blast radius, not a hypothetical one: `test/fixtures/theme/{sections/hero,blocks/button}.liquid`, both inline schema strings in `test/routes/theme.test.ts`, `src/create-site/template/theme/{sections/hero,blocks/button}.liquid` (the real starter theme new sites scaffold from), and every one of the demo theme's 9 sections and 8 blocks in `app-granite-cms-admin/fixtures/demo-site/theme/` all needed a real default added before this could ship, or those types would have silently vanished from every theme that already existed. `test/fixtures/site/theme/` (used only by `layouts.test.ts`/`content-read.test.ts`, neither of which ever touches `themeSchemas`) was confirmed - not assumed - to be unaffected, by grepping for `bootSite`/`themeSchemas` in both consumer test files before deciding to leave it alone.
- **The default must validate against its own property's sub-schema, not just be present** - checked by running Ajv against the property's own JSON Schema fragment with the declared default as the data (`requiredFieldsHaveValidDefaults`). A bare presence check would have let a theme author satisfy L1 with `"default": ""` against a `"minLength": 1` field, which would still hand the admin an invalid pre-filled instance - defeating L3 by construction rather than accident.
- **Pre-fill (L3) is deliberately not limited to required fields** - `buildDefaultSettings` copies in whatever `"default"` a schema property declares, required or not, since there was no reason for an optional field with a genuinely useful default (e.g. `button`'s `style` enum) to be withheld from pre-fill just because it isn't required.
- **Skip-not-crash was a deliberate choice, confirmed with the project owner before implementation**, matching the exact precedent Group K already established for `allowedBlocks` misuse and the pre-existing malformed-`{% schema %}`-block behaviour: a single theme-author mistake makes one type temporarily unavailable in the admin, not the whole site unreachable.

## Group M: auto-generated publish message

Admin-repo-only (no agent API change - `/v1/publish`'s `message` field already accepted arbitrary text). The project owner found typing a commit message on every publish annoying and asked for it to be generated automatically instead, confirmed directly: skip the prompt entirely (no pre-fill-and-edit step), built from the page/menu's own title and today's date.

| # | Criterion | Proof |
|---|---|---|
| M1 | Publishing no longer prompts for a message - it's built automatically from what's being published and today's date, in the form `"<label> - <D Mon YYYY>"` (a plain hyphen, not an em/en dash, per this repo's own no-dash rule for user-facing strings) | `app-granite-cms-admin`: `packages/web/src/editor/publishMessage.ts`'s `buildPublishMessage`; `packages/web/src/pages/derivePageLabel.ts`'s `derivePageLabel` (prefers a page's `name`, falls back to `title`, then its path - menus reuse the existing `deriveMenuName`); `useDraftPublishActions.ts` no longer calls `window.prompt` at all. `publishMessage.test.ts`, `derivePageLabel.test.ts`, `useDraftPublishActions.test.ts`'s rewritten publish cases. Verified live: published the real demo site's `pages/docs.json` through the actual admin -> agent chain with no dialog appearing, and confirmed the resulting git commit in `.demo-runtime` reads exactly `Docs - 11 Aug 2026` |

Design notes:

- **The label is the page's own "name" (Group J), not its raw content path** - confirmed directly with the project owner (the alternative, `pages/about.json - 11 Aug 2026`, needs no new data but reads far worse in publish history).
- **Menus reuse `deriveMenuName.ts` unchanged** - it already existed for exactly this "readable label from a path" purpose (originally for menu display), so no new menu-side code was needed, only wiring it into the publish call.

## Group N: draft-save reliability fixes

Not part of any planned group - three real bugs found live while manually verifying Group M above (publishing a real page kept failing), diagnosed and fixed together since the first two directly compound into the third. None are caused by Group M's own change (confirmed by diffing against the pre-Group-M commit before starting) - all three predate it and were simply easier to trigger once the prompt's incidental multi-second pause (which used to give a pending autosave time to finish) was removed.

| # | Criterion | Proof |
|---|---|---|
| N1 | Content saved before "name" became a required page field (J1) no longer fails validation on every subsequent edit - "name" is backfilled from "title" right before a page (never a post or menu) is saved | `app-granite-cms-admin`: `derivePageLabel.ts`'s `backfillPageName`; `PageEditorPage.tsx`'s `setContent` wrapper (skipped for `posts/` paths - `post.schema.json` has no `"name"` property and rejects one as unknown, matching the agent's own path-prefix content-type dispatch). `derivePageLabel.test.ts`'s six cases; `PageEditorPage.test.tsx`'s posts-excluded and old-shaped-page-still-saves cases. Verified live against the real demo site's `pages/index.json` (genuinely missing "name"): the unmodified payload was confirmed to still 400 with `must have required property 'name'`, then the backfilled payload was confirmed to save with a real 200 |
| N2 | A failed save or publish surfaces its real reason, not a generic HTTP status phrase - "Bad Request" and "Conflict" were never more informative than the specific message they were masking | `app-granite-cms-admin`: `site-editor.ts`'s `reasonFromResponse` now prefers `message` over `error` - applies everywhere in the app that uses it (save, publish, discard, slug rename, theme schemas, history), not just the save path that surfaced it. `PageMetadataPanel.test.tsx`'s slug-rename-conflict case updated from asserting the old generic `"Conflict"` text to the real `"A page already exists at that path"` |
| N3 | Save Changes can't be clicked while the last save failed or the draft has an unresolved conflict - previously this let a doomed publish attempt fire against a draft that was never actually saved, surfacing a second, more confusing error ("This draft no longer exists") on top of the first | `app-granite-cms-admin`: `PageEditorPage.tsx`/`MenuEditorPage.tsx`'s Save Changes `disabled` condition now also covers `status === 'save-error'` and `status === 'conflict'`. `PageEditorPage.test.tsx`'s new case |

Design notes:

- **N1's backfill runs on every edit, not once on load** - deliberately, so that merely opening an old page never silently creates a draft the user didn't ask for; only an actual edit (through any of the three content-editing surfaces - Page tab, Sections tab, raw JSON) triggers it, and only that edit's own save is affected.
- **N2 was scoped to "fix it everywhere," not just the save path** - confirmed directly with the project owner after finding one existing test (`PageMetadataPanel.test.tsx`) that had explicitly documented the old generic-phrase-wins behaviour as "established... not something this feature changes." That documented intent was overridden on the reasoning that a generic HTTP status phrase is never more useful than the specific reason it was hiding, in any flow.
- **A separate, still-open issue found during N's own live verification, not fixed here**: publishing the real demo site's `pages/about-the-company.json` fails with a genuine agent-side 500 (not a validation 400 - its draft saves without error). Not yet diagnosed; likely related to that page's earlier slug-rename history (J3). Flagged for whoever picks this up next, not chased down here since it's unrelated to N1-N3's own root cause.
- **Nothing in Group M or N (or K/L above) has been committed yet** - all of it exists only as uncommitted working-tree changes in both repos as of this note.
